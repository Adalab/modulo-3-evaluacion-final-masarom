const FilterErrorMsg = () => {
    const renderfilterCharacters = () => {
    if (filteredCharacters.length === 0) {
      return <p>We weren't able to find what you're looking for :(</p>;
    }
}
  return 
    {renderfilterCharacters()}
}


export default FilterErrorMsg;